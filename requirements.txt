Flask==2.0.2  # Flask version adjusted to a stable release
Flask-SQLAlchemy==3.1.1
numpy==1.22.4  # Downgraded to a version more widely compatible
pandas==1.3.4  # Adjusted to a stable version
scikit-learn==0.24.2  # A stable version with broader compatibility
scipy==1.7.3  # Updated to a compatible version
seaborn==0.11.2  # A widely compatible version
spacy==3.1.4  # Adjusted to a stable version
SQLAlchemy==1.4.25  # A stable version with broader compatibility
torch==1.10.0  # Downgraded to a version compatible with more environments
torchvision==0.11.1  # Adjusted to a stable version
transformers==4.11.3  # A widely compatible version
